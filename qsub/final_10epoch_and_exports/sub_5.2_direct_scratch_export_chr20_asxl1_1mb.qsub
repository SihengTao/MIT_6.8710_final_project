#!/bin/bash
#$ -N five2_direct_asxl1
#$ -cwd
#$ -l h_vmem=128G
#$ -l h_rt=48:00:00
#$ -l gpu=1
#$ -l os=RedHat8
#$ -hard
#$ -t 1-2
#$ -m ea
#$ -M stao@broadinstitute.org

set -euo pipefail

CODE_DIR="/broad/boxialab/sihengtao/chromnitron/chromnitron_finetune"
PY="/broad/boxialab/sihengtao/env_all/chromnitron_gpu/bin/python"
EXPORT_SCRIPT="${CODE_DIR}/scripts/export_hic2_best_adapter_bigwig_1mb.py"
CONFIG_ROOT="${CODE_DIR}/configs/hic2_bcl11a_export"
LOG_DIR="/broad/boxialab/sihengtao/projects/chromnitron_finetune/logs"

CELLTYPE="check4hic2_anewPaper_single_sample"
ASSEMBLY="hg38"
REGION="chr20:31898825-32898825"
GENE="ASXL1"
TASK_ID="${SGE_TASK_ID:-}"

mkdir -p "${LOG_DIR}"
JOB_TAG="${JOB_ID:-local}.${TASK_ID:-unset}"
RUN_LOG="${LOG_DIR}/five2_direct_asxl1.${JOB_TAG}.log"
exec >"${RUN_LOG}" 2>&1

if [[ ! "${TASK_ID}" =~ ^[0-9]+$ ]] || (( TASK_ID < 1 || TASK_ID > 2 )); then
  echo "Unsupported SGE_TASK_ID=${TASK_ID:-unset}; expected 1-2" >&2
  exit 2
fi

cd "${CODE_DIR}"

export PYTHONUNBUFFERED=1
export SINGULARITY_BINDPATH=/broad/boxialab/sihengtao:/broad/boxialab/sihengtao

RUN_DIRS=(
  ""
  "/broad/boxialab/sihengtao/projects/chromnitron_finetune/gata1_from_baseonly_scratch_r4_lr1e4_ep10_2024paper/20260502-154138"
  "/broad/boxialab/sihengtao/projects/chromnitron_finetune/hic2_from_baseonly_scratch_r4_lr1e4_ep10_2024paper/20260502-154138"
)

CAPS=(
  ""
  "GATA1"
  "HIC2"
)

RUN_DIR="${RUN_DIRS[${TASK_ID}]}"
CAP="${CAPS[${TASK_ID}]}"
OUTPUT_DIR="${RUN_DIR}/hic2_1mb_bigwig_chr20_asxl1"
EXPECTED_BW="${OUTPUT_DIR}/prediction/prediction/${CELLTYPE}/${CAP}/bigwigs/${CAP}_${CELLTYPE}_prediction.bw"

missing_configs=()
for config_name in infer.yaml data_config.yaml finetune_base.yaml; do
  [[ -f "${CONFIG_ROOT}/${config_name}" ]] || missing_configs+=("${CONFIG_ROOT}/${config_name}")
done
if (( ${#missing_configs[@]} > 0 )); then
  echo "Missing export config file(s): ${missing_configs[*]}" >&2
  exit 2
fi

if [[ ! -x "${PY}" ]]; then
  echo "Python executable not found or not executable: ${PY}" >&2
  exit 2
fi

if [[ ! -f "${EXPORT_SCRIPT}" ]]; then
  echo "Export script not found: ${EXPORT_SCRIPT}" >&2
  exit 2
fi

if [[ ! -d "${RUN_DIR}" ]]; then
  echo "Run directory not found: ${RUN_DIR}" >&2
  exit 2
fi

echo "Starting direct scratch ${GENE} 1Mb bigWig export"
echo "  timestamp=$(date -Is)"
echo "  host=$(hostname)"
echo "  job_id=${JOB_ID:-local}"
echo "  task_id=${TASK_ID}"
echo "  run_dir=${RUN_DIR}"
echo "  output_dir=${OUTPUT_DIR}"
echo "  config_root=${CONFIG_ROOT}"
echo "  celltype=${CELLTYPE}"
echo "  cap=${CAP}"
echo "  assembly=${ASSEMBLY}"
echo "  region=${REGION}"
echo "  expected_bigwig=${EXPECTED_BW}"

CMD=(
  "${PY}"
  "${EXPORT_SCRIPT}"
  --run-dir "${RUN_DIR}"
  --config-root "${CONFIG_ROOT}"
  --celltype "${CELLTYPE}"
  --cap "${CAP}"
  --assembly "${ASSEMBLY}"
  --region "${REGION}"
  --output-dir "${OUTPUT_DIR}"
)

echo "Command:"
printf '  %q' "${CMD[@]}"
printf '\n'

"${CMD[@]}"

echo "Finished direct scratch ${GENE} 1Mb bigWig export"
echo "Expected prediction bigWig path: ${EXPECTED_BW}"
